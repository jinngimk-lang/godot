# Peel Calm reference convergence checkpoint 66 — Window Café source-of-truth loop

Date: 2026-08-17

## Owner operating directive

The project is now explicitly long-horizon and gate-driven:

- do not set an artificial iteration-count ceiling;
- do not stop because several passes already happened;
- keep looping while a clear, verifiable, high-value gap remains;
- when context becomes complex/long, write a Git checkpoint and recover from repository evidence rather than chat memory;
- every recovery starts from latest `main`, latest checkpoint, active branches/PRs/CI, and the latest runtime screenshot artifact;
- do not repeat experiments already falsified by prior evidence;
- do not claim visual perfection from machine-green tests;
- final subjective aesthetics remain an owner playtest gate;
- stop only for release gates, explicit owner pause/direction change, true legal/payment/credential/irreversible/high-risk decisions, or a real external capability/permission block.

## Source-of-truth priority

When evidence conflicts, use this order:

1. owner-approved reference images and latest owner visual feedback;
2. the active owner operating directive;
3. latest Git checkpoint;
4. merged product specifications;
5. current implementation;
6. historical prototype behavior.

The locked acceptance set remains `cafe_v1`, `bar_v1`, `market_v1`. Runtime captures are evidence, never target replacements.

## Phase-1 target: `cafe_v1`

Window Café / Paper Cup is now the first completion target. Required visual feel:

- warm natural window light and believable café depth;
- real wood table;
- hero paper cup centered in the middle foreground but not oversized;
- realistic hands with substantial palm/finger volume;
- support hand naturally wraps the cup;
- peel hand thumb/index actually pinch the lifted paper flap;
- matte fibrous paper cup/label;
- molded black lid detail;
- post-peel paper-cup crumple ritual remains playable.

Phase 1 is not considered complete until fresh project screenshots materially converge on the locked `cafe_v1` reference and the owner playtest gate is satisfied.

## Fresh recovery evidence

- latest main before this checkpoint: `694af542407ea648f11501540a79fdf11f15a2cf`;
- previous checkpoint: 65, merged product baseline `6a743b562883eedb4227da5bc1cdfb21c232a286`;
- fresh merged-main Godot Check from checkpoint 65: `32006233403` — PASS;
- fresh runtime artifact: `9280118093`, digest `sha256:9e734def1cbc64bbb41ec936d4db4d80acf7653c7bde1e0d30f72924063009ba`;
- actual locked Library source re-read: `/Peel Calm/Acceptance References/v1/cafe_reference.png`, 2,082,113 bytes, matching manifest role `cafe_v1`.

## Fresh Café comparison

Latest runtime Café still diverges strongly from the locked reference:

1. **R1 hand anatomy / enclosure — largest visual gap.** Hands read as faceted game meshes; support hand does not naturally wrap the cup; long programmatic sleeves/forearms read as tubes.
2. **Cup/lid composition — independent high-value executable gap.** Runtime cup is too large and cylindrical, with a weak/incorrect top silhouette; reference uses a slimmer tapered paper cup and a clearly molded black plastic lid.
3. **Material realism — executable gap.** Cup and hand surfaces remain too uniform/simple relative to the reference's paper fiber, skin micro-surface, soft specular response and cloth.
4. Background/window/table direction is substantially closer than the foreground and is not the current first target.

## R1 capability audit

A fresh plugin search for `Blender`, `3D rigging`, or `3D modeling` returned no installable plugin in the current ChatGPT runtime.

The repository already contains two CC0 rigged Godot XR Tools hand GLBs (`hand_left.glb`, `hand_right.glb`, ~878 KB each) with 26-bone armatures and authored `Cup` / `Pinch Tight` actions. The project wraps them with simple local PBR materials and adds programmatic wrist/sleeve geometry.

The previous stop condition remains valid: do not resume CCD/endpoint chasing, wrist/orbit grids, per-digit transform sweeps, or disguised numeric pose search without live visual native-rig authoring or a structurally better hand source.

This does **not** block fixing independent presentation causes such as sleeve geometry, hand material/lighting, cup/lid geometry, paper material, camera/product scale, or other non-bone composition problems.

## Do not repeat

- do not reopen blind numeric hand pose search;
- do not mistake a green unit test for reference convergence;
- do not replace `cafe_v1` with a runtime or generated convenience target;
- do not declare Phase 1 complete while the hand/cup silhouette still visibly disagrees with the locked reference;
- do not spend new loops on already-accepted label backing/residue work unless fresh evidence exposes a new defect.

## Next exact action

1. Treat the hand bone pose as externally authoring-blocked, not solved.
2. Close the largest independent executable Café foreground gap: slimmer/tapered hero cup + real black molded lid silhouette + less tubular sleeve/forearm presentation, without touching gameplay authority or blind bone search.
3. Add a RED visual/presentation contract for Café cup/lid proportions and sleeve silhouette before production changes.
4. Run exact-head Godot Check and inspect fresh `cafe.png` + `cafe_peel38.png` against `cafe_v1`.
5. If visually safer, run independent Challenger and merge unchanged head; otherwise reject and continue on the same red.
6. Write the next checkpoint before changing red category or when context becomes complex again.
